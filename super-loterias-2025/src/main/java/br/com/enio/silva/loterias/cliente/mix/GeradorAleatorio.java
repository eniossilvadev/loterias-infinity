package br.com.enio.silva.loterias.cliente.mix;

public class GeradorAleatorio {

	//	public static List<List<Integer>> completarExcluirIncluir(List<List<Integer>> lists, int tam,
	//			int maxNum, List<Integer> excluir, List<Integer> incluir, boolean clean) {
	//
	//		final List<Integer> lista = IntStream.rangeClosed(1, maxNum) .boxed().collect(Collectors.toList());
	//
	//		List<Integer> listaAtual = null;
	//		lists.forEach(l -> {
	//			int currSize = tam - l.size();
	//			int pos;
	//			if(listaAtual == null || pos > lista.size()) {
	//				pos = pos % currSize;
	//			}
	//			for(int i = 0; i < currSize; i++) {
	//
	//			}
	//		});
	//		return null;
	//	}

}
